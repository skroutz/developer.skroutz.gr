#!/usr/bin/env ruby
# coding: utf-8

# Usage: bodies [recordings_directory]

require 'yaml'

lookup_dir = ARGV[1] || '../output/responses/'
Dir.glob(lookup_dir + '*.yml').each do |f|
  yml = YAML.load_file(f)
  File.open(File.basename("#{f}.json", '.yml'), 'w') { |out| out << yml[:body] }
end
