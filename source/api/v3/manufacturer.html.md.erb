---
parent: apiv3
title: manufacturer
---

# Manufacturer <%= edit_link %>

<%= partial 'partials/toc' %>

## List manufacturers

<pre class="terminal">
  GET /manufacturers
</pre>

<%= render_recording :manufacturers_index %>

## Retrieve a single manufacturer

<pre class="terminal">
  GET /manufacturers/:id
</pre>

<%= render_recording :manufacturers_show %>

## Retrieve a manufacturer's categories

<pre class="terminal">
  GET /manufacturers/:id/categories
</pre>

<%= render_recording :manufacturer_categories %>

### Ordering

Valid parameters to perform ordering

Name        | Type   | Value                                                    | Description
----------- | ------ | -------------------------------------------------------- | ------------
`order_by`  | String | `name`, `popularity` <small class="mark">default</small> | Order by name or popularity
`order_dir` | String | `asc`, `desc` <small class="mark">default</small>        | Order ascending or descending

## Retrieve a manufacturer's SKUs

<pre class="terminal">
  GET /manufacturers/:id/skus
</pre>

<%= render_recording :manufacturer_skus %>

### Ordering

Valid parameters to perform ordering

Name        | Type   | Value                                                     | Description
----------- | ------ | --------------------------------------------------------- | ------------
`order_by`  | String | `price`, `popularity` <small class="mark">default</small> | Order by price or popularity
`order_dir` | String | `asc`, `desc` <small class="mark">default</small>         | Order ascending or descending
