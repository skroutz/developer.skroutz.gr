---
title: User | API v2
---
# User

* toc
{:toc}

> You need a `full` access token to perform User API calls.

## Bookmarklists
Bookmarklists of user

**Example:**

<pre class="terminal">
GET /bookmarklists
</pre>

**Returns:**

 * `bookmarklists`

## Add Bookmark

### Parameters
 * `type` - `sku` or `product` or `book`
 * `id` - The `id` of Sku/product/book

**Example:**

<pre class="terminal">
GET /bookmark/create&type=sku&id=145
</pre>

**Returns:**

 * `bookmark`

**Errors:**

 * `not_found` - Sku/Product was not found
 * `bookmark_create` - An unexpected error occurred


## Delete Bookmark

### Parameters
 * `type` - `sku` or `product` or `book`
 * `id` - The `id` of Sku/product/book

**Example:**

<pre class="terminal">
GET /bookmark/destroy&type=sku&id=145
</pre>

**Returns:**

 * `success` - true

**Errors:**

 * `not_found` - Bookmark was not found

