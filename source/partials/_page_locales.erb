<% if localized?(current_page) %>
  <div class="alert alert-info">
    <p>
      <span><%= t('common.available_locales') %></span>
      <%= render_inline(available_locales, 'nav-lang nav-lang-inline') %>
    </p>
  </div>
<% end %>
